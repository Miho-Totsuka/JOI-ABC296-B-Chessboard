## 概要
8×8の盤面（文字列8行）から '*' の位置を探し、チェス座標（a1〜h8）で出力する。

## 方針
上から順に全探索し、列は "abcdefgh"[i]、行は (8 - row) に変換する。

## 計算量
O(64)

## メモ
行番号の変換（8 - row）を間違えやすいので、座標の向き（下が1）を意識した。
最初、a1の向きを勘違いしてしまったので、行番号は 8 - row に直して確認した。
